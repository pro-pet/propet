# 开发环境共享配置（提交到 git，不要放敏感信息）
# 敏感信息请放到 .env.development.local 中（已被 gitignore）
NODE_ENV=development

# 后端
PORT=8077

# 数据库连接（在 .env.development.local 中覆盖为真实值）
DATABASE_URL="postgresql://user:password@localhost:5432/propet"
DIRECT_URL="postgresql://user:password@localhost:5432/propet"

JWT_SECRET=change-me-in-local
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:3000

# 前端
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_APP_NAME=Propet Dev
